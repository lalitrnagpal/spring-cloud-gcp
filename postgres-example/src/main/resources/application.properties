server.port=9000

spring.cloud.gcp.sql.database-name=guestbook
spring.cloud.gcp.sql.instance-connection-name=rosy-spring-280908:us-west1:spring-gcp-postgres-test

# So app starts despite "table already exists" errors.
spring.datasource.continue-on-error=true
# Enforces database initialization
spring.datasource.initialization-mode=always

# Leave empty for root, uncomment and fill out if you specified a user
spring.datasource.username=postgres

# Uncomment if root password is specified
spring.datasource.password=mypassword

#spring.cloud.gcp.project-id=
#spring.cloud.gcp.credentials.location=..////..////..////my-service-account.json
